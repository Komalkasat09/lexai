# Phase 4 + Phase 5 Progress (2026-03-18)

## Scope Completed in This Update

### Phase 4: Data/Schema Pipeline + Dashboard Reliability

Implemented:
- Canonical per-query artifacts now generated by `evaluation/run_evaluation.py`.
- Schema parser in `evaluation/prepare_dashboard_data.py` rewritten for actual `complete_results` structure.
- Silent zero fallback removed in `evaluation/results_dashboard.py`.
- Dashboard now fails fast on missing/empty required columns.
- Figure-level provenance sidecars added with row count and non-null metric checks.

Code changes:
- `evaluation/run_evaluation.py`
  - Added canonical per-query export step: `export_per_query_metrics(...)`.
  - Writes per-system CSVs under `output_dir/per_query_metrics/`.
  - Writes `output_dir/per_query_metrics/manifest.json` with row counts.
  - Added strict row-count and schema checks.
- `evaluation/prepare_dashboard_data.py`
  - Now reads latest or explicit `complete_results_*.json`.
  - Requires canonical per-query files from sibling `per_query_metrics/` directory.
  - Validates required columns and writes `dashboard_data_provenance.json`.
- `evaluation/results_dashboard.py`
  - Removed fallback that used zeros when metric columns were missing.
  - Enforced fail-fast checks for required columns and usable data.
  - Calibration plot now uses `ccs_correctness` labels.
  - Writes provenance JSON sidecar for each generated figure:
    - `main_comparison.png.provenance.json`
    - `category_breakdown.png.provenance.json`
    - `calibration.png.provenance.json`
- `evaluation/metrics_engine.py`
  - Added centralized CCS correctness function `_is_response_correct_for_ccs(...)`.
  - `compute_confidence_calibration(...)` now uses this shared logic.

### Phase 4: Small Query Set Validation Run

Run executed:
- Script: `backend/scripts/phase4_small_eval.py`
- Ground truth: `evaluation/ground_truth_verified_393_ready.xlsx`
- Query count: 5 (smoke subset)
- Systems: LexAI, NoRAG, SimpleRAG

Artifacts produced:
- `evaluation/results_phase4_small_eval/complete_results_20260318_192004.json`
- `evaluation/results_phase4_small_eval/per_query_metrics/lexai_per_query_metrics.csv` (5 rows)
- `evaluation/results_phase4_small_eval/per_query_metrics/norag_per_query_metrics.csv` (5 rows)
- `evaluation/results_phase4_small_eval/per_query_metrics/simplerag_per_query_metrics.csv` (5 rows)
- `evaluation/results_phase4_small_eval/per_query_metrics/manifest.json`

Dashboard pipeline validation (strict mode):
- `prepare_dashboard_data.py` completed successfully for small run.
- `results_dashboard.py` generated figures successfully with provenance sidecars.

Outcome against Phase 4 exit criteria:
- No empty/flat charts from schema issues: PASS
- Every generated figure has provenance + row counts: PASS

## Phase 5: Implementation Started

### Threshold ablation credibility upgrades (`evaluation/threshold_ablation.py`)

Implemented:
- Holdout-only policy enforced (no overlap fallback).
- Main-eval query ID extraction now maps checkpoint query text to ground-truth query IDs to prevent leakage.
- Replaced simplistic metric objective inputs with `MetricsEngine.compute_all_metrics(...)` outputs.
- Objective now computed from normalized CAR/HR/AP derived from `MetricsEngine`.

### BNS ablation credibility upgrades (`evaluation/bns_ablation.py`)

Implemented:
- Reports both:
  - Intention-to-answer (all query responses)
  - Conditional-on-answer (answered subset)
- Added attrition accounting per condition:
  - total, answered, abstained/uncertain, errors
- Switched significance tests to paired tests on matched query sets (all + answered intersections), avoiding weak unpaired fallback.
- Updated JSON summary keys accordingly.

## Remaining Work to Fully Close Phase 5

- Run full threshold ablation with a true holdout split and publish selected thresholds.
- Run full BNS ablation with new paired/matched reporting and produce updated figure + table for paper.
- Add paper-ready attrition table export (CSV/LaTeX) if needed by manuscript pipeline.
